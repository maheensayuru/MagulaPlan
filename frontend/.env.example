VITE_API_URL=http://localhost:8080

# Optional, local dev only. Put this in .env.local (not this file) to preview
# /admin without a real admin role from the backend. Never active in a
# production build. See src/components/auth/AdminRoute.jsx.
# VITE_ADMIN_PREVIEW=true
